name: Learn_workflow
on: 
  push:
    branches: [ "main" ]
  
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: simple
        run: echo " runnig success............................."
      - name: build docker image
        run: docker build -t sree471/learn2:latest .
      - name: show docker images
        run: docker images
        
